<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.annaru.upms.mapper.OrderCustomerMapper">

	<!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.annaru.upms.entity.OrderCustomer" >
        <result column="sys_id" property="sysId" />
        <result column="order_no" property="orderNo" />
        <result column="customer_name" property="customerName" />
        <result column="id_no" property="idNo" />
        <result column="gender" property="gender" />
        <result column="cellphone_no1" property="cellphoneNo1" />
        <result column="cellphone_no2" property="cellphoneNo2" />
        <result column="seqno" property="seqno" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 多表页面信息查询分页 -->
    <select id="selectDataPage" parameterType="map" resultMap="BaseResultMap">

        SELECT
              sys_id,
              order_no,
              customer_name,
              id_no,
              gender,
              cellphone_no1,
              cellphone_no2,
              seqno,
              remark
        FROM
            order_customer
        <where>
            <if test="params.customerName != null and params.customerName != ''">
                AND customer_name LIKE CONCAT('%', #{params.customerName}, '%')
            </if>
        </where>
    </select>

    <insert id="insertOrderCustomer" parameterType="map">
        insert into order_customer
                (customer_name,id_no,gender,cellphone_no1)
        values
                (#{params.customer_name},#{params.id_no},#{params.gender},#{params.cellphone_no1})
    </insert>


</mapper>
